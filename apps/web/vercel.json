{
  "installCommand": "pnpm install --frozen-lockfile --filter @doai/web --filter @doai/shared --filter @doai/supabase",
  "buildCommand": "turbo run build",
  "crons": [
    { "path": "/api/cron/sync-channels", "schedule": "* * * * *" },
    { "path": "/api/cron/dispatch-queue", "schedule": "* * * * *" }
  ]
}
