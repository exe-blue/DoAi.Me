# Dashboard GIF assets

Place the following files in this directory (copy from jh-exe-blue/v0-theme-dashboard `public/assets/` if needed):

- **pc_blueprint.gif** — current time block (Widget) background
- **bot_greenprint.gif** — status block (SecurityStatus) background

Until these files exist, the dashboard components will render with a fallback placeholder.
