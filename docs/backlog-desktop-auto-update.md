# Backlog: Desktop Auto-Update

Epic, Feature, Task breakdown. Per task: inputs/outputs, domain events, test strategy, rollback/retry. Responsibility: desktop main, preload, renderer, web Next, server.
