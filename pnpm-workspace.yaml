packages:
  - "apps/*"
  - "packages/*"

# Allow these deps to run install/build scripts (pnpm 10+). Prevents "Ignored build scripts" on Vercel.
onlyBuiltDependencies:
  - "@sentry/cli"
  - "unrs-resolver"
  - "electron"
  - "electron-winstaller"
  - "esbuild"
  - "sharp"
